apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flipkart-ingress
  namespace: flipkart-dev
spec:
  rules:
  - host: dev-flipstore.elsondevops.cloud
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: dev-flipkart-app-service
            port:
              number: 80
